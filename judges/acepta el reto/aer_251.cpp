#include <iostream>
#include <set>
using namespace std;

int main(){
    int casos;
    cin >> casos;
    while(casos--){
        int n, m, monedas[10], count = 0;
        cin >> n >> m;
        for(int i = 0; i < n; i++) {
            cin >> monedas[i];
        }
        for(int i = 0; i < )
    }
}